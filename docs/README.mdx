import { MdView } from '../src'

import readme from '../README.md?raw'

<style>
  {`pre{${
    Object.entries({
      padding: '16px',
      background: '#f6f8fa',
      border: '1px solid #ddd',
      'border-radius': '6px',
      margin: '.5em 0'
    })
      .map((entry) => (`${entry[0]}:${entry[1]} !important;`))
      .join('')
  }}`}
</style>

<MdView copy>{readme}</MdView>
